// Stripe Payment Links are public URLs, not API secrets. Keep both values empty
// until the live links and the public legal/accounting copy have been verified.
window.VORRIO_SUPPORT_LINKS = Object.freeze({
  oneTime: '',
  monthly: '',
})
